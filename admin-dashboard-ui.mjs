import {db} from './admin-db.mjs';import {MODULES,dashboardAccess,dashboardCounts} from './dashboard-management.mjs';
const preferred=new URLSearchParams(location.search).get('church'),$=s=>document.querySelector(s);let access,church;const el=(tag,value='',cls='')=>{const n=document.createElement(tag);n.textContent=value;n.className=cls;return n;};
function href(file){return file+'?church='+encodeURIComponent(church);}
function renderChurches(){const nav=$('#churches');nav.replaceChildren();for(const value of access.churches){const a=el('a',value==='M+'?'M＋大雅教會':'火樂教會');a.href='admin-dashboard.html?church='+encodeURIComponent(value);if(value===church)a.className='current';nav.append(a);}}
function renderModules(){const area=$('#modules');area.replaceChildren();const grants=new Set(access.grants.filter(g=>g.church_id===church).map(g=>g.permission));for(const module of MODULES.filter(m=>grants.has(m.permission))){const a=document.createElement('a');a.href=href(module.file);a.className='dashboard-card';a.append(el('span',module.icon,'dashboard-icon'),el('h3',module.title),el('p',module.description));area.append(a);}if(!area.children.length)area.append(el('p','此堂會目前沒有可用功能。','muted'));document.querySelector('.system-links a').href=href('admin-accounts.html');}
function renderCounts(counts){const area=$('#summary');area.replaceChildren();for(const [key,label] of [['members','會員'],['groups','小組／小家'],['prayers','待關懷代禱'],['bookings','待審核預約']]){if(counts[key]===undefined)continue;const card=el('article','','summary-card');card.append(el('strong',counts[key]===null?'—':String(counts[key])),el('span',label));area.append(card);}area.hidden=!area.children.length;}
async function load(){try{({access,church}=await dashboardAccess(db,preferred));$('#title').textContent=(church==='M+'?'M＋大雅教會':'火樂教會')+' · 管理首頁';$('#welcome').textContent='登入帳號：'+access.user.name;renderChurches();renderModules();const counts=await dashboardCounts(db,church,access.grants);renderCounts(counts);$('#status').textContent='已依您的權限顯示可使用功能。';}catch(error){$('#status').textContent=error.message;$('#logout').textContent='回登入頁';$('#logout').onclick=()=>location.replace('admin-login.html');}}
$('#logout').onclick=async()=>{await db.auth.signOut();location.replace('admin-login.html');};load();
